# 修正版 v2 — 2026-09-16

## 実装した変更

- ギャラリーを GalleryV2.jsx に置き換え。画像と作品名・矢印を同じテクスチャに描き、同じ曲面に載せる。常時の波打ちを除去し、移動速度に応じる変形と停止時の収束を実装。
- フレーム間隔に基づく追従へ変更。画像テクスチャ更新は最大25fpsに抑え、カメラ・カードの移動は毎フレーム更新。
- FullIndex.jsx を追加。作品名のホバーでプレビューを表示、ポインター追従・傾き・作品切り替え・離脱時の縮小を実装。3作品に合わせて複数行の文字組みへ変更。
- Profileの輪を物理ベースの反射材質へ変更。作品画像を環境反射に使用し、周囲のギャラリー画像を歪める描画を追加。マウス位置に応じて輪が傾く。内部に回転する立体オブジェクトを追加。
- ProjectBody.jsx を追加。スクロール速度に応じて右側の作品面が傾き、停止すると戻る。説明と閉じるボタンは固定。開く位置に応じた展開アニメーションを追加。
- 3作品すべてにImageGenで生成した別カットを追加。ギャラリーとホバープレビューは2カットを使ったゆっくりしたズーム・パン・クロスフェードで動く。
- 作品詳細の列比率・余白・文字組みを修正。同じ画像の再掲を別カットへ置換。

## デモとしての範囲

参照サイトの動画や3Dモデルは使用していない。映像部分は生成静止画を動かす構成であり、被写体そのものが動く実写動画ではない。輪の材質・内部モデル・詳細の変形は独自実装で、参照と完全に同じ演算や挙動ではない。実名・実績・連絡先は引き続き仮設定。

## 保存と再現

- 起動: START.cmd。Node.jsが必要。初期設定では http://127.0.0.1:4173/ を開く。
- 現在の確認用サーバー: http://127.0.0.1:4174/
- ソース: app/src。実際に使用するギャラリーは GalleryV2.jsx。
- ビルド済み: app/dist/client。
- 再ビルド: appで npm ci、npm run build。
- 修正前ソース: audit-2026-09-16/src-before-fix。
- 修正後のスクリーンショット: evidence-v2。
- アーカイブ作成: python archive.py。ZIP内のCRCも検証。

## 検証

1280×720と390×844でギャラリー、Full、作品詳細、Profileを実画面確認。ホバー表示・作品切り替え・開閉・詳細スクロール・次作品・デモフォームを操作。ビルド成功。WebGLの浮動小数点精度に関するドライバー警告は出るが、確認したログにアプリ実行エラーはない。長時間稼働や端末ごとのFPS計測は未実施。
