# 新規Claude Codeセッションへ

これは「WEBサイト課題の提出サイト」です。最初に必ず以下を読んでください。

1. `README.md`
2. `AGENTS.md`
3. `docs/引き継ぎ.md`

その後、目的に応じて `app/`（公開画面）または `apps-script/`（Gmail→Sheets監視）を作業対象にしてください。実運用の識別子・認可情報は公開リポジトリにはありません。必要なときだけ運用者にアクセスを確認し、`ops.local.md`（Git管理外）を参照してください。秘密値をコード・ログ・コミットに出力しないでください。
